Тестовое задание 

Для Linux:
bash
python3 -m venv .venv
source .venv/bin/activate
pip install -U pip
pip install -r requirements.txt
python main.py

Задача выполнена согласно ТЗ:
Двойным кликом можно менять имя
ПКМ можно добавить дочерний элемент 
В колонке image выводится изображение
Реализована раскраска ячеек


Доп команды что посмотреть данные в консоле:
python main.py --schema
python main.py --roots
python main.py --images

